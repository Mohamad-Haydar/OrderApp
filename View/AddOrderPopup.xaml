<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
                xmlns:model="clr-namespace:OrderApp.Model"
                xmlns:viewmodel="clr-namespace:OrderApp.ViewModel"
                x:DataType="viewmodel:AddOrderPopupViewModel"
                x:Class="OrderApp.View.AddOrderPopup">
    <VerticalStackLayout Padding="20" Spacing="15">
        <Label Text="Create New Order" FontAttributes="Bold" FontSize="20" HorizontalOptions="Center"/>

        <!-- Client Picker -->
        <Label Text="Select Client:"/>
        <Picker Title="Select a client"
                ItemsSource="{Binding Clients}"
                ItemDisplayBinding="{Binding Name}"
                SelectedItem="{Binding Client}">
        </Picker>

        <!-- Date Selection -->
        <Label Text="Select Date:"/>
        <DatePicker Date="{Binding Order.DateToPick}"
                    MinimumDate="{Binding Today}"
                    Format="D"/>

        <!-- Buttons -->
        <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
            <Button Text="Save"  Command="{Binding AddOrderCommand}" WidthRequest="90"/>
            <Button Text="Cancel" Clicked="CloseButton_Clicked" WidthRequest="90"/>
        </HorizontalStackLayout>
    </VerticalStackLayout>
</toolkit:Popup>